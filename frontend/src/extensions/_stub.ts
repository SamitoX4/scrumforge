/**
 * Stub vacío para extensiones premium no instaladas.
 * Vite resuelve este módulo cuando la extensión no existe en disco.
 * loadFrontendExtensions() detecta que no hay export válido y lo ignora silenciosamente.
 */
export default null;
